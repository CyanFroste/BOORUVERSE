server side
    Refactoring
    'Preview' feature expansion and implementing manual cache 
    ? save full post in bookmarks
    ? replace download websocket system with a promise 
    ? rethrow db operation errors
    backup / restore bookmarks
    start mongoDB when server starts using script
    bookmarks
        add fields: checked, following
    delete file
    '+' in URL is being parsed as space

client side
    define functions outside components if possible
    reuse paginator functions
    preview controls
    handle empty result errors
    bookmarks
        option to copy id and tag easily, control checked, follow status
    